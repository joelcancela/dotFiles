# clink

- Use portable version of clink

```bash
clink autorun install -- --quiet
```

- Put `.inputrc` in `%USERPROFILE%`
- Put the rest of the files in `clink` folder
- Set `%CLINK_SETTINGS%` env var to the `clink` folder location